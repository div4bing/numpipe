To compile the kernel module, run "make".
To insert the kernel module and create a device node run "sh run.sh"
To modify FIFO size, open run.sh and modify the passing parameters in it and run "sh run.sh"
